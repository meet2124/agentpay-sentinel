# AgentPay Sentinel — Backend Dependencies
# Python 3.12 compatible
# These are PRODUCTION dependencies — included in the Lambda deployment package.
# Test/dev dependencies live in requirements-dev.txt.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Data validation
pydantic==2.9.2
pydantic-settings==2.5.2

# String matching (payee similarity)
rapidfuzz==3.10.0

# File uploads
python-multipart==0.0.12

# AWS SDK (boto3 stubs in local mode; real usage in AWS mode)
boto3==1.35.28

# AWS Lambda ↔ ASGI adapter (wraps FastAPI for Lambda invocation)
# mangum is NOT imported in local/uvicorn mode — imported only in lambda_handler.py
mangum==0.19.0

# Dev tools
python-dotenv==1.0.1
